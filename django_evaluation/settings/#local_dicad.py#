"""
Local settings for cmip-eval.dkrz.de
"""
from .base import *
print 'using mistral settings'
import os
import logging

import ldap

import django_auth_ldap.config as ldap_cfg
from django_auth_ldap.config import LDAPSearch, PosixGroupType

#from django_evaluation.ldapnisgroup import LDAPNisGroupType

PROJECT_ROOT = os.path.abspath(os.path.dirname(__file__) + "../../../")

# Path to logo
STATIC_URL = '/static/'
INSTITUTION_LOGO = STATIC_URL + 'img/dicad_logo.png'
INSTITUTION_NAME = 'DICAD'
FREVA_LOGO = STATIC_URL + 'img/by_freva_transparent.png'
#MAIN_COLOR = '#f29631'
MAIN_COLOR = '#473c8b'
HOVER_COLOR = '#4fa9fb'
BORDER_COLOR = '#473c8b'

##########
#Here you can customize the footer and texts on the startpage
##########
ABOUT_US_TEXT = '''The <a href="https://redmine.dkrz.de/projects/cmip6-dicad" style='color:#F29631'target="_blank">CMIP-DICAD</a> project provides a German contribution within the CMIP6 Project.The objective of <a href="https://www.wcrp-climate.org/wgcm-cmip/wgcm-cmip6" style='color:#F29631'target="_blank">CMIP6</a> is to better understand past, present and future climate change arising from natural, unforced variability or in response to changes in radiative forcings in a multi-model context.'''

CONTACTS = ['christopher.kadow@met.fu-berlin.de (freva)',
	    'thomas.schartner@met.fu-berlin.de (freva)',
            'Veronika.Eyring@dlr.de (ESMValTool PI)',
            'Axel.Lauer@dlr.de (ESMValTool Core Developer)']
IMPRINT = ['Freva - INTEGRATION', 'Institut fur Meteorologie - FU Berlin',
           'Carl-Heinrich-Becker-Weg 6-10', '12165 Berlin']
HOMEPAGE_HEADING = '''<a href="http://www.esmvaltool.org/" style='color:#F29631' target="_blank">ESMValTool</a>.'''
HOMEPAGE_TEXT = '''The Earth System Model Evaluation Tool (ESMValTool) is a community-developed diagnostic and performance metrics tool for the evaluation of Earth system models with observations. It includes other well-established model evaluation packages such as the NCAR Climate Variability Diagnostics Package (CVDP). The collection of standard namelists for example also allows to reproduce the figures from the climate model evaluation chapter of IPCC AR5 (Chapter 9) and parts of the projection chapter (Chapter 12). ESMValTool is available as open source on <a href="https://github.com/ESMValGroup/ESMValTool" style='color:#F29631' target="_blank">GitHub</a>.'''

##################################################
##################################################
#SETTING FOR LDAP
#http://pythonhosted.org//django-auth-ldap/
##################################################
##################################################
# The server for LDAP configuration
AUTH_LDAP_SERVER_URI = "ldaps://dsp.dkrz.de"
# The directory with SSL certificates
CA_CERT_DIR = '/etc/pki/tls/certs'
# the only allowd group
ALLOWED_GROUP = 'mistraluser'
# Require a ca certificate
AUTH_LDAP_GLOBAL_OPTIONS = {
    ldap.OPT_X_TLS_REQUIRE_CERT: ldap.OPT_X_TLS_DEMAND, #TPYE OF CERTIFICATION
    ldap.OPT_X_TLS_CACERTDIR: CA_CERT_DIR, #PATH OF CERTIFICATION
}
# this is not used by django directly, but we use it for
# python-ldap access, as well.
LDAP_USER_BASE = "ou=People,o=ldap,o=root"
# Verify the user by bind to LDAP
AUTH_LDAP_USER_DN_TEMPLATE = "uid=%%(user)s, %s" % LDAP_USER_BASE
# keep the authenticated user for group search
AUTH_LDAP_BIND_AS_AUTHENTICATING_USER=True
# dicad user only
AUTH_LDAP_REQUIRE_GROUP = "cn=mistraluser,ou=group,o=ldap,o=root"  # ALLOWED_GROUP
# find only users from group dicad
# AUTH_LDAP_USER_SEARCH = LDAPSearch("ou=netgroup,o=ldap,o=root"
#     ldap.SCOPE_SUBTREE,
#     '&((cn=%s)(nisNetgroupTriple=\(,%%(user)s,\)))' % ALLOWED_GROUP )
# Populate the Django user from the LDAP directory.
AUTH_LDAP_USER_ATTR_MAP = {
    "email": "mail",
    "last_name" : "sn",
    "first_name" : "givenname",
}

AUTH_LDAP_GROUP_SEARCH = LDAPSearch('ou=group,o=ldap,o=root',
                                     ldap.SCOPE_SUBTREE, #USE SUB
                                     '(objectClass=posixGroup)')
#AUTH_LDAP_GROUP_TYPE = ldap_cfg.ActiveDirectoryGroupType()
AUTH_LDAP_GROUP_TYPE = PosixGroupType()
# AUTH_LDAP_FIND_GROUP_PERMS = True
AUTH_LDAP_MIRROR_GROUPS = True
# agent user for LDAP
LDAP_USER_DN = 'cn=dkrzagent, ou=Special Users, dc=adm'
LDAP_USER_PW = 'dkrzprox'
LDAP_GROUP_BASE = 'ou=group, o=ldap, o=root'
LDAP_MIKLIP_GROUP_FILTER = '(cn=mistraluser)'
LDAP_MODEL = 'django_evaluation.ldaptools.MiklipUserInformation'
##################################################
##################################################
#END SETTING FOR LDAP
##################################################
##################################################
AUTHENTICATION_BACKENDS = (
    'django.contrib.auth.backends.ModelBackend',
    'django_auth_ldap.backend.LDAPBackend',
)

# the host to start the scheduler
SCHEDULER_HOSTS=['mistral.dkrz.de']
# temporary directory for tailed scheduler files
TAIL_TMP_DIR = '/tmp/tail_offset/'

# Additional locations of static files
STATICFILES_DIRS = (
    # Put strings here, like "/home/html/static" or "C:/www/django/static".
    # Always use forward slashes, even on Windows.
    # Don't forget to use absolute paths, not relative paths.
    os.path.join(PROJECT_ROOT, 'assets/dist'),
    ('preview', '/var/www/freva/cmip6-dicad/misc4freva/db4freva/preview'),
)

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'freva',
        'USER': 'freva',
        'PASSWORD': 'zowebezi', #'miklip',
        'HOST': 'localhost', #'wwwdev-miklip',
        'PORT': '3306',
    },
}

# SECURITY WARNING: don't run with debug turned on in production!
# Debugging displays nice error messages, but leaks memory. Set this to False
# on all server instances and True only for development.
DEBUG = TEMPLATE_DEBUG = True

# Is this a development instance? Set this to True on development/master
# instances and False on stage/prod.
DEV = True

# Hosts/domain names that are valid for this site; required if DEBUG is False
# See https://docs.djangoproject.com/en/1.5/ref/settings/#allowed-hosts
ALLOWED_HOSTS = ['cmip-eval.dkrz.de','cmip-esmvaltool.dkrz.de']

# path to the site packages used:
VENV_PYTHON_DIR = '/var/www/freva/cmip6-dicad/freva_web/vepy/bin/python'

# Path to miklip-logo
STATIC_URL = '/static/'
MIKLIP_LOGO = STATIC_URL + 'img/miklip-logo.png'

NCDUMP_BINARY = '/sw/rhel6-x64/netcdf/netcdf_c-4.3.2-gcc48/bin/ncdump'

LOAD_MODULE='source /mnt/lustre02/work/dicad/cmip6-dicad/misc4freva/loadscripts/loadfreva.source > /dev/null;'

# result to show at guest tour
#GUEST_TOUR_RESULT = 17508

WEBPACK_LOADER = {
    'DEFAULT': {
        'BUNDLE_DIR_NAME': '/static/',
        'STATS_FILE': os.path.join(PROJECT_ROOT, 'webpack-stats-prod.json'),
    }
}

RESULT_BROWSER_FACETS = ['ESMValTool namelists', 'Projects', 'CMIP6 Realms', 'Themes', 'Domain', 'Plot Type', 'Statistics',
                          'References', 'Variables', 'Models']

CMIP6_PATH = '/history/cmip6-results/'

# MENU_ENTRIES
MENU_ENTRIES = [
    {'name':'Home', 'url': reverse_lazy('base:home'),'html_id': 'home_menu'},
    {'name':'Result-Browser','url': reverse_lazy('history:result_browser'), 'html_id': 'history_menu', 'required' : False},
   # {'name':'CMIP6 Results','url': reverse_lazy('history:cmip6_result_browser'), 'html_id': 'contact_menu'},
    {'name':'CMIP6 Results','url': '/tab/terms/', 'html_id': 'contact_menu'},
    {'name':'Data-Browser', 'url': reverse_lazy('solr:data_browser'), 'html_id': 'browser_menu',},
    {'name':'Feedback','url': reverse_lazy('base:contact'), 'html_id': 'contact_menu'},
    {'name':'Terms of Use','url': '/tab/terms/', 'html_id': 'contact_menu'},
    {'name':'ESMValTool Info','url': '/tab/esmval/', 'html_id': 'contact_menu'},
    {'name':'Help','url': reverse_lazy('plugins:about'), 'html_id': 'doc_menu'},
]

#Freva for CMIP6DICAD? Set evc to true
EVC = True

SETTINGS_EXPORT = [
   'EVC',
]

